{
  "name": "@ananta/auth-client",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "jwks-rsa": "^3.1.0",
    "@fastify/auth": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^20.0.0",
    "fastify": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
